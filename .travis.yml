language: php

php:
  - 7.2

addons:
mariadb: '10.4'

before_install:
  - cd galeno-app
  - mysql -e 'CREATE DATABASE galeno;'
before_script:
  - cp .env.travis .env
  - composer install --no-interaction
  - php artisan key:generate

cache:
  directories:
    - vendor
